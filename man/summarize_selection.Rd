% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_selection.R
\name{summarize_selection}
\alias{summarize_selection}
\title{Decompose phenotype variance across selected pathways}
\usage{
summarize_selection(
  sel_result,
  expr,
  phenotype,
  gene_sets,
  score = c("mean_z", "pc1")
)
}
\arguments{
\item{sel_result}{Data frame returned by
\code{\link{run_assoc_redundancy_selection}} or
\code{\link{classify_assoc_selection}}.}

\item{expr}{Numeric matrix of expression values, \strong{samples x genes}.
Must be the same matrix used in \code{\link{run_info_assoc}}.}

\item{phenotype}{Vector of per-sample phenotype values. Must be the same
vector used in \code{\link{run_info_assoc}}.}

\item{gene_sets}{Named list of character vectors defining gene set
membership. Must contain all pathways listed in
\code{sel_result$pathway}.}

\item{score}{Character. Activity scoring method: \code{"mean_z"}
(default) or \code{"pc1"}. Should match the method used in
\code{\link{run_info_assoc}}.}
}
\value{
The input data frame with three additional columns:
\describe{
\item{\code{marginal_r2}}{Univariate \eqn{R^2}: variance explained
by this pathway alone.}
\item{\code{incremental_r2}}{Incremental \eqn{R^2}: new variance
explained at this selection step, conditional on all earlier
selections. Equals \code{marginal_r2} at step 1.}
\item{\code{cumulative_r2}}{Cumulative \eqn{R^2}: total variance
explained by pathways 1 through \emph{k}.}
}
Attributes:
\describe{
\item{\code{total_r2}}{Final cumulative \eqn{R^2} (same as last
row of \code{cumulative_r2}).}
\item{\code{adj_r2}}{Adjusted \eqn{R^2} for the full model.}
\item{\code{residual_pct}}{Percentage of variance \emph{not}
explained by the selected set.}
}
}
\description{
Adds variance-explained columns to the output of
\code{\link{run_assoc_redundancy_selection}} or
\code{\link{classify_assoc_selection}}, translating information-theoretic
quantities into familiar R-squared values. This is the summary table
that answers: "how much of the phenotype does each independent pathway
explain, and how much is left over?"
}
\section{Method}{

For each selection step \emph{k}, a linear model
\code{phenotype ~ activity_1 + ... + activity_k} is fitted. The
incremental \eqn{R^2} at step \emph{k} is the gain from adding
pathway \emph{k} to the model containing pathways 1 through \emph{k-1}.
This parallels the \code{conditional_mi} column: both measure the
unique contribution of each pathway given all earlier selections. The
difference is that \eqn{R^2} assumes a linear relationship and is
expressed as a proportion of variance, while MI is non-parametric and
expressed in bits.
}

\section{Interpretation}{

\itemize{
\item \code{marginal_r2}: how much phenotype variance this pathway
explains alone (without controlling for other pathways).
\item \code{incremental_r2}: how much \emph{new} variance this pathway
adds beyond all previously selected pathways. A redundant pathway
contributes near-zero incremental \eqn{R^2}.
\item \code{cumulative_r2}: total variance explained by the first
\emph{k} selected pathways. The final row gives the total explained
by the non-redundant set.
}
}

\examples{
set.seed(42)
n <- 120; genes <- paste0("G", 1:300)
fA <- rnorm(n); fB <- rnorm(n)
expr <- matrix(rnorm(n * 300), nrow = n, dimnames = list(NULL, genes))
expr[, 1:30]    <- expr[, 1:30]    + 2.5 * fA
expr[, 200:230] <- expr[, 200:230] + 2.0 * fB
phenotype <- 0.7 * fA + 0.5 * fB + rnorm(n, sd = 0.4)

gene_sets <- list(
  pathway_A  = genes[1:30],
  pathway_A2 = genes[5:35],
  pathway_B  = genes[200:230],
  noise      = genes[100:130]
)

assoc <- run_info_assoc(expr, phenotype, gene_sets, n_perm = 500, seed = 42)
sel   <- run_assoc_redundancy_selection(
  assoc, expr, phenotype, gene_sets, seed = 42
)

result <- summarize_selection(sel, expr, phenotype, gene_sets)
print(result[, c("pathway", "signal_class", "marginal_r2",
                  "incremental_r2", "cumulative_r2")])
cat(sprintf("Total R2: \%.1f\%\%  |  Residual: \%.1f\%\%\n",
            attr(result, "total_r2") * 100,
            attr(result, "residual_pct")))

}
\seealso{
\code{\link{run_assoc_redundancy_selection}} for the upstream
selection step, \code{\link{classify_assoc_selection}} for signal
classification, \code{\link{compute_pathway_activity}} for the
underlying activity scores.
}
